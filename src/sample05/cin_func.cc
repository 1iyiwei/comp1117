#include <iostream>
#include <cmath>
using namespace std;

int main(void)
{
    float x;

    cout << (cin >> x) << endl;

    // cout << fabs(cin >> x) << endl; // compiler error
    
    return 1;
}
